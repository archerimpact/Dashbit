<nav class='navbar navbar-default'>
    <div class='container-fluid'>
        <div class='navbar-header'>
            <a class='navbar-brand' href='/folders'>Dashbit</a>
        </div>
        <div class="collapse navbar-collapse">
            <div class="navbar-right">
                <% if (currentUser) { %>
                     <a id="logoutBtn" class="btn btn-outline-success my-2 my-sm-0" href='/logout'>Logout</a>
                <% } else { %>
                    <a id='logoutBtn' class='btn btn-outline-success my-2 my-sm-0' href='/login'>Login or Register</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>

<div class='container'>
    <% if (error && error.length > 0) { %>
        <div class='alert alert-danger' role='alert'>
            <%= error %>
        </div>
    <% } %>
    <% if (success && success.length > 0) { %>
        <div class='alert alert-success' role='alert'>
            <%= success %>
        </div>
    <% } %>
</div>